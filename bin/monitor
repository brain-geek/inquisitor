#!/usr/bin/env ruby
require 'rubygems'
require File.expand_path(File.dirname(__FILE__), '..', 'lib', 'monit')

#TODO: get me from options!
DataMapper.setup(:default, "sqlite3://#{Dir.pwd}/../development.db")
DataMapper.auto_upgrade!

Monit::Node.all.each do |node|
  node.check_and_notify
end