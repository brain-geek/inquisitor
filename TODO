- Make package at rubygems
- Automated, scheduled running in daemon(eventmashine?)
- Resque worker
- Tests for runner
- Logging results
- Daily reports about uptime of services(?)
- Support for several 'monitor' nodes with one web UI